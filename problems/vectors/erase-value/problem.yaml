title: Remove every copy of a value
prompt: |
  Remove every element equal to `x` from `v`, keeping the order of the rest. `v.size()` must shrink.
given: |
  std::vector<int> v;  // e.g. {1, 2, 3, 2, 4, 2}
  int x;               // e.g. 2  ->  v becomes {1, 3, 4}
