title: Copy a slice into a new vector
prompt: |
  Declare `std::vector<int> sub` holding the elements of `v` from index 2 up to, but not including, index 5.
given: |
  std::vector<int> v;  // at least 5 elements, e.g. {10, 20, 30, 40, 50, 60}
